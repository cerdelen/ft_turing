use std::error::Error;

pub struct MachineDescription {
}

impl MachineDescription {
    pub fn new(input: &str) -> Self {
        // let _ = serde_json::from_str(input).expect("Invalid Json");
        MachineDescription{}
    }
}
